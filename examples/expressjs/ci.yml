name: CI

on:
  pull_request:
    branches: [main, develop]  # Run CI on PRs to both branches
    paths-ignore:
      - '**.md'
      - 'docs/**'
  push:
    branches: [main, develop]  # Run CI on pushes to both branches
    paths-ignore:
      - '**.md'
      - 'docs/**'

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  ci:
    uses: aladagemre/devops/.github/workflows/expressjs-ci.yml@main
    with:
      node-version: '20'
      skip-typecheck: false
      skip-lint: false
      skip-tests: false
